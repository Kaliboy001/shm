{
  "name": "shm",
  "version": "1.0.0",
  "description": "A Cloudflare Worker proxy for gpt.lovetoome.com API",
  "main": "src/index.js",
  "scripts": {
    "build": "npx wrangler deploy --minify --dry-run --outdir=./dist"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
